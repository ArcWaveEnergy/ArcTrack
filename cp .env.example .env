SMTP_HOST=your.mailserver.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_SECURE=false      # set true if using port 465 (SSL)
MAIL_FROM="ArcTrack Reports <no-reply@arcwaveenergy.com>"
npm install
npm run db:init
